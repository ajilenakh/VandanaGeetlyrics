# Quick Fixes

> **For agentic workers:** REQUIRED SUB-SKILL: Use `executing-plans` to implement this plan task-by-task.

**Goal:** Fix stale references in AGENTS.md to match current codebase state.

---

## Task 1: Remove song-header from Components list

**Files:**
- Modify: `AGENTS.md`

- [ ] **Step 1: Update Components line**

Line 16 currently says: `Components: src/_includes/components/ — header, footer, song-header`

Remove `song-header` — it was deleted and all pages now use the universal `header.njk`.

**Before:**
```markdown
- **Components**: `src/_includes/components/` — header, footer, song-header
```

**After:**
```markdown
- **Components**: `src/_includes/components/` — header, footer
```

- [ ] **Step 2: Commit**

```bash
git add AGENTS.md && git commit -m "fix: remove stale song-header reference from AGENTS.md"
```